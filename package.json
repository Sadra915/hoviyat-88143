{
  "name": "hoviyat",
  "version": "1.0.0",
  "private": true,
  "description": "هویت — پیام‌رسان. این package.json فقط برای ابزار بسته‌بندی Capacitor (تبدیل به اپ اندروید) است؛ خود وب‌اپ هیچ build step یا bundler ندارد و همان‌طور که هست کار می‌کند.",
  "scripts": {
    "build:dist": "node build-dist.mjs",
    "app:add-android": "npm run build:dist && npx cap add android --config capacitor.config.app.json && node scripts/patch-android-deeplink.mjs",
    "app:sync": "npm run build:dist && npx cap sync android --config capacitor.config.app.json && node scripts/patch-android-deeplink.mjs",
    "app:open": "npx cap open android --config capacitor.config.app.json"
  },
  "dependencies": {
    "@capacitor/android": "^6.1.2",
    "@capacitor/core": "^6.1.2",
    "@capacitor/app": "^6.1.2",
    "@capacitor/browser": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.1.2"
  }
}
