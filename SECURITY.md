# امنیت هویت — یادداشت مهاجرت شماره ۳

این فایل خلاصه‌ی صادقانه‌ی چیزی‌ست که در `supabase/migration_3_security_hardening.sql`
و کدهای همراهش تغییر کرده، چرا، و چه چیزهایی *عمداً* اضافه نشده‌اند.

## ۱) باگ بحرانی که رفع شد

قبلاً هر کاربر واردشده می‌توانست کل جدول `profiles` (شامل **شماره تلفن**) را
بدون داشتن یوزرنیم/آیدی کسی ببیند (`listAllUsers()` + یک RLS policy که به همه‌ی
کاربرانِ authenticated اجازه‌ی select کامل می‌داد). این دقیقاً همان چیزی بود که
گزارش شده بود.

**رفع شد با:**
- RLS جدید روی `profiles`: فقط ردیف خودت، یا کسی که با او در یک چت/گروه مشترک
  هستی، قابل خواندن مستقیم است.
- تابع `search_profile_by_username()`: تنها راه پیدا کردن یک غریبه — فقط با
  تطابق دقیق یوزرنیم، فقط فیلدهای عمومی (بدون تلفن)، با محدودیت نرخ (ضد کاوش).
- حذف کامل `listAllUsers()` از کد؛ لیست اعضای گروه حالا از مخاطب‌های موجود
  (چت‌های باز) + جستجوی دقیق یوزرنیم تشکیل می‌شود.

## ۲) رمزگذاری پیام‌های خصوصی — چیزی که هست و چیزی که نیست

متن پیام‌های چت خصوصی (۱به۱) اکنون در دیتابیس با `pgcrypto` رمزنگاری می‌شود
(«رمزگذاری در حالت سکون»، ستون `body_enc`).

**این E2E واقعی نیست** — کلید رمزگذاری سمت سرور نگه‌داری می‌شود (نه روی
دستگاه کاربر)، چون پنل مدیریت (`admin.js`) باید بتواند پیام‌های *گزارش‌شده*
را برای بررسی تخلف بخواند. اگر کلید فقط دست کاربر باشد، این قابلیت مدیریتی از
بین می‌رود. اگر مایلید به‌سمت E2E واقعی بروید (تبادل کلید بین دو کاربر با
Web Crypto، ذخیره‌ی کلید خصوصی روی دستگاه، پشتیبان‌گیری/بازیابی چندجهته)،
این یک پروژه‌ی جداگانه‌ی بزرگ‌تر است که باید آگاهانه با از دست دادن قابلیت
بازبینی گزارش‌ها توسط ادمین (یا طراحی مکانیزم دیگری برای گزارش‌ها) همراه شود.

پیام‌های گروه/کانال فعلاً رمزنگاری نشده‌اند (چون فضای اشتراکی‌اند و مدیران
گروه هم نیاز به مشاهده دارند)؛ اگر خواستید همین الگو برای آن‌ها هم اجرا شود.

## ۳) اضافه‌شده در این مهاجرت

- قفل برنامه: PIN (هش‌شده با bcrypt سمت سرور) + بیومتریک (WebAuthn، محلیِ دستگاه).
- ۲مرحله‌ای (TOTP) — پیاده‌سازی کامل بدون کتابخانه‌ی خارجی.
- نشست‌ها/دستگاه‌ها: لیست، برچسب دستگاه، «خروج از راه دور».
- تاریخچه‌ی ورود + پرچم «دستگاه جدید».
- کد بازیابی برای بازگشایی قفل/۲مرحله‌ای در صورت گم‌کردن دستگاه.
- محدودسازی نرخ: ارسال پیام (ضد اسپم)، جستجوی یوزرنیم (ضد کاوش)، تلاش PIN (ضد بروت‌فورس).
- پاک‌سازی خودکار پیام‌ها بر اساس ترجیح کاربر (نیاز به `pg_cron` یا Scheduled Function دارد — پایین را ببینید).
- ضدفیشینگ ساده: لینک‌های داخل پیام با چند قانون ابتدایی (Punycode، کوتاه‌کننده، IP خام، دامنه‌ی مشابه برند) پرچم می‌خورند و قبل از باز شدن هشدار نشان داده می‌شود.
- «امتیاز امنیت»: یک چک‌لیست ساده و قانون‌محور — **نه** هوش مصنوعی/یادگیری ماشین واقعی.

## ۴) کاری که باید دستی انجام دهید

- **اجرای مهاجرت**: فایل `supabase/migration_3_security_hardening.sql` را در
  Supabase SQL Editor اجرا کنید (بعد از migration_2).
- **زمان‌بندی پاک‌سازی خودکار**: اگر پروژه‌ی شما `pg_cron` دارد:
  ```sql
  select cron.schedule('cleanup-old-messages', '0 3 * * *',
    $$select public.cleanup_expired_messages()$$);
  ```
  در غیر این صورت، این تابع را از یک Supabase Edge Function زمان‌بندی‌شده صدا بزنید.
- **خروج از راه دورِ واقعی**: `revoke_session()` فقط رکورد نشست را حذف می‌کند
  (اپ در آن دستگاه در اولین تماس متوجه می‌شود). برای باطل‌کردن فوریِ توکن JWT
  خود Supabase Auth، به یک Edge Function با `service_role` نیاز دارید — آن
  کلید هرگز نباید در کد کلاینت باشد.

## ۵) یک مشکل دیگر که پیدا شد ولی هنوز رفع نشده (نیاز به تصمیم شما دارد)

باکت‌های Storage برای عکس/صدا (`chat-media`, `group-media`) روی **Public**
تنظیم شده‌اند. در Supabase Storage، حالت public یعنی هرکس *لینک مستقیم* فایل
را داشته باشد (حتی بدون لاگین) می‌تواند آن را ببیند — چون این مسیر از RLS
عبور می‌کند. یعنی اگر یک لینک عکسِ چت خصوصی جایی درز کند (کش مرورگر، لاگ
پروکسی، و...)، هرکس با آن لینک می‌تواند آن را ببیند.

**راه‌حل درست:** باکت‌ها را Private کنید و به‌جای `getPublicUrl`، از
`createSignedUrl` (با انقضای کوتاه) استفاده کنید. این یک تغییر نسبتاً بزرگ در
مسیر رندر پیام‌هاست (چند فایل: `chat.js`, `groups.js`, `channels.js`, `ui.js`)
و چون امکان تست زنده در این محیط وجود نداشت (بدون دسترسی شبکه)، ترجیح دادم
آن را با جزئیات کامل مستند کنم تا با عجله و بدون تست چیزی را نشکنم. اگر
می‌خواهید، در یک پاس جداگانه با دقت انجامش می‌دهم.

## ۶) موارد درخواستی که پیاده‌سازی نشدند (و چرا)

این‌ها یا برنامه/فرآیند سازمانی‌اند (نه کد)، یا نیاز به زیرساخت بیشتری دارند
که فراتر از یک مهاجرت امن و قابل‌بازبینی بود:

- **برنامه Bug Bounty / آزمایش امنیت عمومی / گزارش شفاف امنیت** — این‌ها
  فرآیندهای سازمانی‌اند، نه یک فایل کد. توصیه: یک صفحه‌ی عمومی با ایمیل تماس
  امنیتی + سیاست افشای مسئولانه بسازید.
- **کلید امنیتی سخت‌افزاری** — با همان WebAuthn که برای بیومتریک اضافه شده
  پوشش داده می‌شود (YubiKey و مشابه هم از طریق `authenticatorAttachment`
  می‌توانند اضافه شوند)؛ اگر می‌خواهید جدا از قفل محلی، به ورود اصلی هم وصل شود، جای کار دارد.
- **حالت کودکان / کنترل والدین / حالت سفر / مخاطب اضطراری / هشدار تعویض
  سیم‌کارت** — این‌ها ویژگی‌های کامل و مستقل هستند که هرکدام طراحی UX خاص خود
  را می‌طلبند؛ برای اینکه نیمه‌کاره و کم‌کیفیت اضافه نشوند، فعلاً کنار گذاشته
  شدند.
- **تشخیص ربات/هوش مصنوعی واقعی** — آنچه اضافه شد قانون‌محور است (نرخ، الگوی
  ساده)؛ یک مدل واقعی نیاز به داده‌ی برچسب‌خورده و زیرساخت ML جداگانه دارد.

## HOVIYAT Moderation V2

Migration `supabase/next/012_group_moderation_v2.sql` adds server-enforced group member moderation metadata, role/permission management, group message restrictions, report workflow fields, and admin audit events. Run migrations in numeric order after the existing base migrations.

Client UI never constitutes the authorization boundary. Group moderation uses the `group_moderation_action` RPC and system moderation uses admin-only RPCs protected by `is_admin()`.
